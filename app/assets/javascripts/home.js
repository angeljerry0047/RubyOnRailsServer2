// NOTE (cmhobbs) why is this commented out?
/*
$(document).on('ready', function() {
  $('.register').bind('click', function(e){
    if(!$('.tc_check').prop('checked')){
      console.log('not checked');
      e.stopPropagation();
      e.preventDefault();
      alert("You must read and accept the terms of use and privacy policy before signing up.");
    }
  });
  if(BrowserDetect.browser == "Explorer" && BrowserDetect.version==8){
	$('.carousel-image').remove();
  } 
});
*/
