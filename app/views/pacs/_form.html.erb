<%= stylesheet_link_tag "application/pac", :media => "all" %>
<%= simple_form_for(@pac) do |f| %>
  <% if @pac.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pac.errors.count, "error") %> prohibited this video from being saved:</h2>

      <ul>
      <% @pac.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<% end %>
