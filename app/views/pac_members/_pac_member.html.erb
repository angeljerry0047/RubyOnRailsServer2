<tr>
  <td><%= pac_member.member %></td>
  <% unless @pac.first.complete? %><td><%= button_to 'Remove from team', pac_member, :class => "btn btn-large btn-primary pac roundedge", method: :delete,
    data: { confirm: 'Are you sure?' } %></td><% end %>
  </tr>