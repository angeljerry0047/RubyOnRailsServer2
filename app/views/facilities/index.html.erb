<%= stylesheet_link_tag "application/fast_content", :media => "all" %>
<center><h1>internal resources listings</h1></center>
<br/>
<br/>
<table>
  <tr class="table-border">
    <th>Topic</th>
    <th>Description</th>
    <th>External link</th>
    <th>Embedded link</th>
    <th>Author</th>
    <th></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @facility.each do |facility| %>
  <tr class="table-border">
    <td><%= facility.topic %></td>
    <td><%= facility.description %></td>
    <td><%= facility.ext_link %></td>
    <td><%= facility.emb_link %></td>
    <td><%= facility.author %></td>
    <td></td>
    <td class="no-border"><%= link_to 'Show', facility %>|</td>
    <td class="no-border"><%= link_to 'Edit', edit_facility_path(facility) %>|</td>
    <td class="no-border"><%= link_to 'Destroy', facility, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Fast content', new_facility_path %>
